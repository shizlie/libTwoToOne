# mesh — prebuilt CLI bundles (v1.1.0-6ba5e5a)

Self-contained `mesh` + `meshl` bundles. Run on any **Bun** or **Node 18+**; no native
binary, nothing to compile.

## Install / update

```sh
bun add -g github:shizlie/libTwoToOne#v1.1.0-6ba5e5a    # pinned to this exact build — always fresh
```

The pinned `#v1.1.0-6ba5e5a` tag is the reliable form: `bun add -g github:shizlie/libTwoToOne` (no ref) can
serve a STALE cached build because Bun caches git ref→commit lookups. If you use the unpinned
form and it looks stale, run `bun pm cache rm` and re-install, or pin the tag above. Re-run to
update. (Add `~/.bun/bin` to your `PATH` if Bun prompts after a global add.)

## Run the live agent demo (optional)

This repo also ships `live-demo.sh` and the two agent operating contracts it loads. With a
logged-in `claude` plus `tmux` and `bun`, stand up two real Claude agents that coordinate
through a room:

```sh
git clone --depth 1 https://github.com/shizlie/libTwoToOne demo && cd demo
ROOM_URL=https://<your-room> bash live-demo.sh        # then: bash live-demo.sh --fire
```

Nothing is scripted — each agent is a live `claude` acting on its contract, woken by its own
`meshl` listener. Tear down with `bash live-demo.sh --clean`.

---

Built from [`shizlie/TwoToOne@6ba5e5a`](https://github.com/shizlie/TwoToOne) (private source). This repo is
generated by `bun run publish:dist` and carries only the built artifacts — open issues / PRs on
the source repo.
